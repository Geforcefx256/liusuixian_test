## 1. 后端上传编码归一化

- [x] 1.1 在 `apps/agent-backend` 的文件上传链路中新增文本编码识别与 UTF-8 归一化逻辑，覆盖 UTF-8、UTF-8 BOM、UTF-16 BOM 和 GB18030-family 文本。
- [x] 1.2 将上传持久化流程调整为仅在成功解码后写入 UTF-8 内容，并在不受支持编码或非文本内容时返回显式错误。
- [x] 1.3 保持工作区打开、保存和下载链路与归一化后的 UTF-8 存储语义一致，避免再依赖原始上传字节编码。

## 2. 工作台文件体验联动

- [x] 2.1 确认 `apps/web` 的工作台文件打开与上传失败处理能够消费新的上传语义，不展示乱码文件为成功打开状态。
- [x] 2.2 验证上传后的中文文本文件在打开、编辑、保存、关闭和再次打开时都保持可读，不需要用户手动选择编码。

## 3. 测试与回归验证

- [x] 3.1 为 `apps/agent-backend` 补充单元/路由测试，覆盖 UTF-8、UTF-8 BOM、UTF-16 BOM、GB18030 和不受支持内容的上传结果。
- [x] 3.2 为工作台相关测试补充上传后打开可读中文文本和上传失败不进入编辑器流的断言。
- [x] 3.3 运行相关类型检查与测试，确认现有工作区上传、打开、保存、下载行为无回归。
