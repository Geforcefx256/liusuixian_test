## 1. 调整重命名输入语义

- [x] 1.1 在 `apps/web` 的 workbench 重命名流程中新增清晰的文件名拆分辅助逻辑，区分叶子文件名、可编辑 stem 和保留 extension
- [x] 1.2 调整工作区文件重命名提示与默认输入值，使有扩展名的文件只暴露 stem 给用户编辑，并在提交前重新组合完整文件名
- [x] 1.3 保持无扩展名文件的完整名称可编辑路径，避免在前端错误追加后缀

## 2. 校验与回归测试

- [x] 2.1 更新 `apps/web/src/stores/workbenchStore.test.ts` 中的重命名用例，验证 prompt 默认值改为 stem，提交值仍为带原扩展名的完整文件名
- [x] 2.2 为无扩展名文件补充重命名测试，验证输入值与提交值都保持完整名称语义
- [x] 2.3 运行相关前端测试，确认已有重命名成功、失败、脏文件阻断和运行中阻断路径未回归
