## 1. SessionRail 交互修复

- [x] 1.1 梳理 `SessionRail` 中菜单、确认层和展开面板的状态切换，定位会在批量清空 `click` 前回收菜单项的关闭路径。
- [x] 1.2 调整 `更多操作 -> 清空历史会话` 的激活与关闭顺序，确保真实鼠标点击能稳定打开批量清空确认框。
- [x] 1.3 修复批量清空菜单项在可用状态下的键盘可达性，保证标准焦点导航和键盘触发可以进入确认框。

## 2. 回归测试

- [x] 2.1 为 `SessionRail` 增加覆盖真实菜单点击链路的组件测试，验证主指针点击批量清空项会弹出确认框而不是直接收起菜单。
- [x] 2.2 为 `SessionRail` 增加键盘焦点与键盘触发测试，验证可用的批量清空项可聚焦、可打开确认框，且确认前不会发请求。
- [x] 2.3 回归检查单条删除、Esc 关闭和点击外部关闭测试，确保修复批量清空交互时不破坏既有危险操作语义。

## 3. 验证

- [x] 3.1 运行 `apps/web` 中与 `SessionRail` 相关的 Vitest 用例，确认历史侧栏交互测试全部通过。
- [ ] 3.2 手动验证历史会话侧栏中的 `清空历史会话` 在真实鼠标点击下会弹出确认框，并在确认后发出批量清空请求。
