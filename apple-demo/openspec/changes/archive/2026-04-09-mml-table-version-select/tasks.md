## 1. 后台 mml-rule 导入逻辑调整

- [x] 1.1 修改规则文件命名解析逻辑，使其按 `CHECK_RULE_网元类型_版本.xlsx` 识别 MML 规则文件。
- [x] 1.2 调整规则导入流程：若数据库中已存在相同 `网元类型 + 网元版本` 的数据，则先删除旧数据，再导入当前文件内容。
- [x] 1.3 在规则文件成功解析并保存到数据库后，删除原始 Excel 文件；若导入失败，则保留源文件。
- [x] 1.4 补充后端测试，覆盖新命名规则、同组合覆盖重载，以及成功导入后删除源文件的行为。

## 2. 前端表格视图下拉交互

- [x] 2.1 增加后台接口与前端 API 调用，用于获取 `网元类型` 和 `网元版本` 的下拉选项数据。
- [x] 2.2 将 MML 表格视图相关配置中的 `网元类型` 和 `网元版本` 改为下拉框，并将默认值设置为 `请选择`。
- [x] 2.3 在用户完成下拉选择后，复用现有 schema 加载和表格视图流程，不再依赖用户手工输入。
- [x] 2.4 补充前端测试，覆盖默认 `请选择`、后台数据驱动的下拉渲染，以及选择后触发表格视图所需加载逻辑。

## 3. 验证

- [x] 3.1 运行相关 `web-backend` 测试，验证规则导入、覆盖重载和源文件删除行为。
- [x] 3.2 运行相关 `web` 测试，验证表格视图中的网元类型/版本下拉交互符合需求。
