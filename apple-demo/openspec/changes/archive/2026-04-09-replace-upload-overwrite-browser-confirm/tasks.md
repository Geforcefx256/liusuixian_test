## 1. 上传冲突确认状态改造

- [x] 1.1 将上传冲突从 `window.confirm` 改为显式的待确认覆盖状态，同时保持现有上传 API 与冲突错误契约不变
- [x] 1.2 让多文件上传在命中冲突时按顺序暂停，并在确认或取消后继续处理剩余文件

## 2. Workbench 确认弹层接入

- [x] 2.1 在 workbench shell 中接入上传冲突确认弹层，复用现有确认表面、遮罩和按钮 token
- [x] 2.2 为上传冲突确认补充文件路径、覆盖后果、取消与确认覆盖文案，并保持点击外部与 `Esc` 关闭语义

## 3. 回归验证

- [x] 3.1 更新前端测试，覆盖上传冲突的打开、取消、确认覆盖以及多文件顺序恢复路径
- [x] 3.2 回归确认现有 workbench 确认交互与上传流程未被新确认弹层改坏
